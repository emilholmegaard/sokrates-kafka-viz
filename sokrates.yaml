analyzers:
  service:
    enabled: true
    include_tests: false
  avro:
    enabled: true
    schema_registry: http://localhost:8081
  kafka:
    enabled: true

output:
  format: json
  path: ./analysis_output